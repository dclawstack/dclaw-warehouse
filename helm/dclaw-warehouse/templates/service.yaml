apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-warehouse.fullname" . }}
  labels:
    {{- include "dclaw-warehouse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-warehouse.selectorLabels" . | nindent 4 }}
