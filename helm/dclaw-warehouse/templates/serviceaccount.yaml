apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dclaw-warehouse.serviceAccountName" . }}
  labels:
    {{- include "dclaw-warehouse.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
